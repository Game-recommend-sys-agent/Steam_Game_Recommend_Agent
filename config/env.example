# main.py 실행 시 필요한 환경 변수 예시
# 이 파일을 복사해서 .env 로 저장한 뒤, 실제 값으로 채운다.
# .env 는 .gitignore 에 있으므로 커밋되지 않음.

# Steam Web API 키 (필수: 최근 플레이·보유 게임 조회)
# 발급: https://steamcommunity.com/dev/apikey
STEAM_API_KEY=your_steam_web_api_key

# 추천 대상 Steam 유저 ID (필수: 이 유저의 플레이 기록으로 컨텍스트 생성)
# 프로필 URL 또는 https://steamid.io/ 에서 확인
STEAM_ID=76561198xxxxxxxx

# IGDB API (Twitch 개발자 콘솔에서 발급, 필수: 게임 메타데이터)
# https://dev.twitch.tv/console/apps
IGDB_CLIENT_ID=your_igdb_client_id
IGDB_CLIENT_SECRET=your_igdb_client_secret

# OpenAI API 키 (필수: LLM 재랭킹)
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your_openai_api_key

# ---------- GameWiki (Fandom) - 게임 설명문 수집용 ----------
# Fandom MediaWiki API 는 읽기용으로 대부분 API 키 불필요.
# 파트너/특정 위키에서 키를 요구하면 발급 후 아래에 넣는다.

# Fandom 위키 서브도메인 (기본: videogaming → videogaming.fandom.com. 게임별: eldenring, minecraft)
FANDOM_WIKI_BASE=videogaming

# Fandom API 키 (선택). 비우면 키 없이 요청. 키 발급받으면 여기 채움.
FANDOM_API_KEY=
